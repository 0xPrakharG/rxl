export const SectionHeader = ({ label }) => {
  return (
    <div className="flex w-fit items-center justify-center rounded-full bg-[#CFEEFF] px-10 py-1 text-center text-base font-bold text-[#026295]">
      {label}
    </div>
  );
};
